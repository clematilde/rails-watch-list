<div class="banner">
  <h1>Lists</h1>
</div>

<div class="container">
  <ul>
    <% @lists.each do |list| %>
      <li>
      <%= list.name %>
      <%= link_to "details", list_path(list) %>
      </li>
    <% end %>
  </ul>

<div>
  <%= link_to "Create a new list", new_list_path %>
</div>

</div>
